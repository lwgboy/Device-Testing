第一次使用firmware-mod-kit會缺少liblzma-dev套件，下載指令如下：
# apt-get install liblzma-dev
解開設備firmmare設定檔
./extract-firmware.sh ~/Downloads/xxx.bin(檔案位置)
解完後會在firmware-mod-kit/fmk 資料夾裡面

binwalk 可以檢查檔案結構
binwalk -e 檔名 可以解開檔案 
 
ls -alh 顯示連結檔案的路徑位置
grep -r admin --color=auto 找關鍵字 admin 用顏色mark
grep -r -niC 5 admin --color=auto 
grep -r -iC 5 pw --color=auto
